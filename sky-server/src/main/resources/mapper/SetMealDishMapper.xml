<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SetMealDishMapper">
    <insert id="insert">
        insert into set_meal_dish(set_meal_id, dish_id, name, price, copies)
        values
        <foreach collection="setMealDishes" item="smd" separator=",">
            (#{smd.setMealId}, #{smd.dishId}, #{smd.name}, #{smd.price}, #{smd.copies})
        </foreach>

    </insert>

    <delete id="deleteBatch">
        delete from set_meal_dish
        where set_meal_id in
        <foreach collection="setMealIds" item="setMealId" separator="," open="(" close=")">
            #{setMealId}
        </foreach>
    </delete>

    <select id="getSetMealByDishId" resultType="java.lang.Long">
        select set_meal_id
        from set_meal_dish
        where dish_id = #{dishId}
    </select>

    <select id="getBySetMealId" resultType="com.sky.entity.SetMealDish">
        select *
        from set_meal_dish
        where set_meal_id = #{setMealId}
    </select>

    <select id="getDishBySetMealId" resultType="com.sky.entity.Dish">
        select d.*
        from set_meal_dish smd join dish d
        on smd.dish_id = d.id
        where smd.set_meal_id = #{setMealId}
    </select>
</mapper>