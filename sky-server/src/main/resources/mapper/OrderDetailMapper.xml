<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail (name, image, order_id, dish_id, set_meal_id, dish_flavor, amount, number)
        values <foreach collection="orderDetailList" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.dishId}, #{od.setMealId}, #{od.dishFlavor}, #{od.amount}, #{od.number})
        </foreach>
    </insert>

    <select id="getByOrderId" resultType="com.sky.entity.OrderDetail">
        select *
        from order_detail
        where order_id = #{orderId}
    </select>
</mapper>