<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
    <insert id="insert">
        insert into shopping_cart (name, image, user_id, dish_id, set_meal_id, dish_flavor, number, amount, create_time)
        values (#{name}, #{image}, #{userId}, #{dishId}, #{setMealId}, #{dishFlavor}, #{number}, #{amount}, #{createTime})
    </insert>

    <insert id="insertBatch">
        insert into shopping_cart (name, image, user_id, dish_id, set_meal_id, dish_flavor, number, amount, create_time)
        values <foreach collection="shoppingCartList" item="o" separator=",">
        (#{o.name}, #{o.image}, #{o.userId}, #{o.dishId}, #{o.setMealId}, #{o.dishFlavor}, #{o.number}, #{o.amount}, #{o.createTime})
    </foreach>
    </insert>

    <update id="updateNumberById">
        update shopping_cart
        set number = #{number}
        where id = #{id}
    </update>

    <delete id="deleteByUserId">
        delete from shopping_cart
        where user_id = #{userId}
    </delete>

    <delete id="deleteById">
        delete from shopping_cart
        where id = #{id}
    </delete>

    <select id="list" resultType="com.sky.entity.ShoppingCart">
        select *
        from shopping_cart
        <where>
            <if test="userId != null">user_id = #{userId}</if>
            <if test="dishId != null">and dish_id = #{dishId} </if>
            <if test="dishFlavor != null">and dish_flavor = #{dishFlavor} </if>
            <if test="setMealId != null">and set_meal_id = #{setMealId} </if>
        </where>
    </select>

    <select id="getByUserId" resultType="com.sky.entity.ShoppingCart">
        select *
        from shopping_cart
        where user_id = #{userId}
    </select>
</mapper>